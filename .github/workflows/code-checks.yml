name: Code Quality

on:
  pull_request:
    branches: [master]
    paths-ignore:
      - "README.md"
  push:
    branches: [master]
    paths-ignore:
      - "README.md"

jobs:
  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          uv sync
          uv run python gen_filter_cache.py

      - name: Ruff lint
        run: uv run ruff check .

      - name: Ruff format check
        run: uv run ruff format --check .

      - name: Ty type check
        run: uv run ty check media_downloader.py utils module
